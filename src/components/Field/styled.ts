import { styled } from "@mui/material";

export const FieldWrap = styled("div")(() => ({
  display: "flex",
  flexDirection: "column",
  marginBottom: "16px",
}));

export const FieldValidationWrap = styled("div")(() => ({
  marginTop: "8px",
}));
